# Task: Add Tailwind styling baseline and Makefile

## Summary
- Configure TailwindCSS + PostCSS and wire global styles for the design system.
- Add a lightweight layout shell and a Makefile for repeatable commands.

**Status:** Completed.

## Acceptance Criteria
- [x] Tailwind uses class-based dark mode and scans `app/`, `components/`, and `content/`.
- [x] `app/globals.css` pulls in Tailwind layers and sets base body styles.
- [x] `Makefile` exposes install, dev, build, lint, typecheck, format, and ci targets.

## Notes
- The layout shell can stay minimal until milestone 2 expands navigation.
