# Task: Scaffold Next.js app with App Router

## Summary
- Initialize a Next.js App Router + TypeScript project with baseline config and scripts.
- Create the core `app/` structure with a root layout and landing page stub.

**Status:** Completed.

## Acceptance Criteria
- [x] `app/` includes `layout.tsx` and `page.tsx` that render without errors.
- [x] `next.config.mjs`, `next-env.d.ts`, and `tsconfig.json` support `pnpm build`.
- [x] `package.json` defines scripts for dev, build, start, lint, typecheck, and format.

## Notes
- Keep the initial scaffold minimal so milestone work can iterate quickly.
